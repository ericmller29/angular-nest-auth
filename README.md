# Angular + NestJS + Mongo Authentication

## Installation

run `npm i`

Update `server/app.module.ts` to the correct location of your mongodb.

*This will probably change*
Update `server/src/auth/constants.ts` to a random string for JWT

## Run
`npm run serve`